#!/bin/bash

# omarchy:summary=Display the Omarchy logo in the terminal using green color.

logo_file=~/.config/omarchy/branding/logo.txt
[[ -f $logo_file ]] || logo_file=$OMARCHY_PATH/logo.txt

clear
echo -e "\033[32m"
cat <"$logo_file"
echo -e "\033[0m"
echo
