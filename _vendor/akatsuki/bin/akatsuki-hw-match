#!/bin/bash

# akatsuki:summary=Match against the computer's DMI product name or product family (case-insensitive).
# akatsuki:args=<pattern>

grep -qi "$1" /sys/class/dmi/id/product_name 2>/dev/null ||
grep -qi "$1" /sys/class/dmi/id/product_family 2>/dev/null
