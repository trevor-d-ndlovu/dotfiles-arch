#!/bin/bash

# akatsuki:summary=Launch the Akatsuki Menu or takes a parameter to jump straight to a submenu.

# Set to true when going directly to a submenu, so we can exit directly
BACK_TO_EXIT=false

back_to() {
  local parent_menu="$1"

  if [[ $BACK_TO_EXIT == "true" ]]; then
    exit 0
  elif [[ -n $parent_menu ]]; then
    "$parent_menu"
  else
    show_main_menu
  fi
}

toggle_existing_menu() {
  if pgrep -f "walker.*--dmenu" >/dev/null; then
    walker --close >/dev/null 2>&1
    exit 0
  fi
}

menu() {
  local prompt="$1"
  local options="$2"
  local extra="$3"
  local preselect="$4"

  read -r -a args <<<"$extra"

  if [[ -n $preselect ]]; then
    local index
    index=$(echo -e "$options" | grep -nxF "$preselect" | cut -d: -f1)
    if [[ -n $index ]]; then
      args+=("-c" "$index")
    fi
  fi

  echo -e "$options" | akatsuki-launch-walker --dmenu --width 295 --minheight 1 --maxheight 630 -p "$prompt…" "${args[@]}" 2>/dev/null
}

terminal() {
  xdg-terminal-exec --app-id=org.akatsuki.terminal "$@"
}

present_terminal() {
  akatsuki-launch-floating-terminal-with-presentation $1
}

open_in_editor() {
  notify-send -u low "Editing config file" "$1"
  akatsuki-launch-editor "$1"
}

install() {
  present_terminal "echo 'Installing $1...'; akatsuki-pkg-add $2"
}

install_and_launch() {
  present_terminal "echo 'Installing $1...'; akatsuki-pkg-add $2 && setsid gtk-launch $3"
}

install_font() {
  present_terminal "echo 'Installing $1...'; akatsuki-pkg-add $2 && sleep 2 && akatsuki-font-set '$3'"
}

install_terminal() {
  present_terminal "akatsuki-install-terminal $1"
}

aur_install() {
  present_terminal "echo 'Installing $1 from AUR...'; akatsuki-pkg-aur-add $2"
}

aur_install_and_launch() {
  present_terminal "echo 'Installing $1 from AUR...'; akatsuki-pkg-aur-add $2 && setsid gtk-launch $3"
}

show_learn_menu() {
  case $(menu "Learn" "  Keybindings\n  Akatsuki\n  Hyprland\n󰣇  Arch\n  Neovim\n󱆃  Bash") in
  *Keybindings*) akatsuki-menu-keybindings ;;
  *Akatsuki*) akatsuki-launch-webapp "https://learn.omacom.io/2/the-akatsuki-manual" ;;
  *Hyprland*) akatsuki-launch-webapp "https://wiki.hypr.land/" ;;
  *Arch*) akatsuki-launch-webapp "https://wiki.archlinux.org/title/Main_page" ;;
  *Bash*) akatsuki-launch-webapp "https://devhints.io/bash" ;;
  *Neovim*) akatsuki-launch-webapp "https://www.lazyvim.org/keymaps" ;;
  *) show_main_menu ;;
  esac
}

show_trigger_menu() {
  case $(menu "Trigger" "󰔛  Reminder\n  Capture\n󰧸  Transcode\n  Share\n󰔎  Toggle\n  Hardware") in
  *Reminder*) show_reminder_menu ;;
  *Capture*) show_capture_menu ;;
  *Transcode*) akatsuki-transcode || back_to show_trigger_menu ;;
  *Share*) show_share_menu ;;
  *Toggle*) show_toggle_menu ;;
  *Hardware*) show_hardware_menu ;;
  *) show_main_menu ;;
  esac
}

show_reminder_menu() {
  case $(menu "Reminder" "󰔛  Set one\n󰔛  Show all\n󰔛  Clear all") in
  *Set*) show_custom_reminder_input ;;
  *"Show all"*) akatsuki-reminder show ;;
  *"Clear all"*) akatsuki-reminder clear ;;
  *) back_to show_trigger_menu ;;
  esac
}

show_custom_reminder_input() {
  local minutes
  minutes=$(akatsuki-menu-input "Remind in minutes")

  if [[ $minutes =~ ^[0-9]+$ ]] && ((minutes > 0)); then
    show_reminder_message_input "$minutes"
  elif [[ -n $minutes ]]; then
    akatsuki-notification-send "󰔛" "Invalid reminder" "Enter the number of minutes" -u critical
    show_custom_reminder_input
  else
    back_to show_reminder_menu
  fi
}

show_reminder_message_input() {
  local minutes="$1"
  local message
  message=$(akatsuki-menu-input "Reminder message")

  if [[ -n $message ]]; then
    akatsuki-reminder "$minutes" "$message"
  else
    akatsuki-reminder "$minutes"
  fi
}

show_capture_menu() {
  case $(menu "Capture" "  Screenshot\n  Screenrecord\n󰴑  Text Extraction\n󰃉  Color") in
  *Screenshot*) akatsuki-capture-screenshot ;;
  *Screenrecord*) show_screenrecord_menu ;;
  *Text*) akatsuki-capture-text-extraction ;;
  *Color*) pkill hyprpicker || hyprpicker -a ;;
  *) back_to show_trigger_menu ;;
  esac
}

get_webcam_list() {
  v4l2-ctl --list-devices 2>/dev/null | while IFS= read -r line; do
    if [[ $line != $'\t'* && -n $line ]]; then
      local name="$line"
      IFS= read -r device || break
      device=$(echo "$device" | tr -d '\t' | head -1)
      [[ -n $device ]] && echo "$device  $name"
    fi
  done
}

show_webcam_select_menu() {
  local devices=$(get_webcam_list)
  local count=$(echo "$devices" | grep -c . 2>/dev/null || echo 0)

  if [[ -z $devices ]] || ((count == 0)); then
    notify-send "No webcam devices found" -u critical -t 3000
    return 1
  fi

  if ((count == 1)); then
    echo "$devices" | awk '{print $1}'
  else
    menu "Select Webcam" "$devices" | awk '{print $1}'
  fi
}

show_screenrecord_menu() {
  akatsuki-capture-screenrecording --stop-recording && exit 0

  case $(menu "Screenrecord" "  With no audio\n  With desktop audio\n  With desktop + microphone audio\n  With desktop + microphone audio + webcam") in
  *"With no audio") akatsuki-capture-screenrecording ;;
  *"With desktop audio") akatsuki-capture-screenrecording --with-desktop-audio ;;
  *"With desktop + microphone audio") akatsuki-capture-screenrecording --with-desktop-audio --with-microphone-audio ;;
  *"With desktop + microphone audio + webcam")
    local device=$(show_webcam_select_menu) || {
      back_to show_capture_menu
      return
    }
    akatsuki-capture-screenrecording --with-desktop-audio --with-microphone-audio --with-webcam --webcam-device="$device"
    ;;
  *) back_to show_capture_menu ;;
  esac
}

show_share_menu() {
  case $(menu "Share" "  Clipboard\n  File \n  Folder") in
  *Clipboard*) akatsuki-menu-share clipboard ;;
  *File*) terminal bash -c "akatsuki-menu-share file" ;;
  *Folder*) terminal bash -c "akatsuki-menu-share folder" ;;
  *) back_to show_trigger_menu ;;
  esac
}

show_toggle_menu() {
  local options="󱄄  Screensaver\n󰔎  Nightlight\n󱫖  Idle Lock\n󰂛  Notifications\n󰍜  Top Bar\n󱂬  Workspace Layout\n  Window Gaps\n  1-Window Ratio\n󰍹  Monitor Scaling\n  Direct Boot\n󰟵  Passwordless Sudo"

  case $(menu "Toggle" "$options") in
  *Screensaver*) akatsuki-toggle-screensaver ;;
  *Nightlight*) akatsuki-toggle-nightlight ;;
  *Idle*) akatsuki-toggle-idle ;;
  *Notifications*) akatsuki-toggle-notification-silencing ;;
  *Bar*) akatsuki-toggle-waybar ;;
  *Layout*) akatsuki-hyprland-workspace-layout-toggle ;;
  *Ratio*) akatsuki-hyprland-window-single-square-aspect-toggle ;;
  *Gaps*) akatsuki-hyprland-window-gaps-toggle ;;
  *Scaling*) akatsuki-hyprland-monitor-scaling-cycle ;;
  *"Direct Boot"*) present_terminal akatsuki-config-direct-boot ;;
  *"Passwordless Sudo"*) present_terminal akatsuki-sudo-passwordless ;;
  *) back_to show_trigger_menu ;;
  esac
}

show_hardware_menu() {
  local options="󰛧  Laptop Display\n 󰍹  Mirror Display"

  if akatsuki-hw-hybrid-gpu; then
    options="$options\n  Hybrid GPU"
  fi

  if akatsuki-hw-touchpad; then
    options="$options\n󰟸  Touchpad"
  fi

  if akatsuki-hw-dell-xps-haptic-touchpad && akatsuki-cmd-present dell-xps-touchpad-haptics; then
    options="$options\n󰌌  Touchpad Haptics"
  fi

  if akatsuki-hw-touchscreen; then
    options="$options\n󰆽  Touchscreen"
  fi

  case $(menu "Toggle" "$options") in
  *Laptop*) akatsuki-hyprland-monitor-internal toggle ;;
  *Mirror*) akatsuki-hyprland-monitor-internal-mirror toggle ;;
  *Haptics*) show_hardware_touchpad_haptics_menu ;;
  *Touchpad*) akatsuki-toggle-touchpad ;;
  *Touchscreen*) akatsuki-toggle-touchscreen ;;
  *"Hybrid GPU"*) present_terminal akatsuki-toggle-hybrid-gpu ;;
  *) back_to show_trigger_menu ;;
  esac
}

show_hardware_touchpad_haptics_menu() {
  local current=$(dell-xps-touchpad-haptics get)
  local selected=$(menu "Touchpad Haptics" "low\nmid\nhigh" "" "$current")

  if [[ -n $selected ]]; then
    dell-xps-touchpad-haptics set "$selected"
  else
    back_to show_hardware_menu
  fi
}

show_style_menu() {
  case $(menu "Style" "󰸌  Theme\n󰟵  Unlock\n  Font\n  Background\n  Hyprland\n󱄄  Screensaver\n  About") in
  *Theme*) show_theme_menu ;;
  *Unlock*) akatsuki-launch-walker -m menus:akatsukiunlocks --width 800 --minheight 400 ;;
  *Font*) show_font_menu ;;
  *Background*) show_background_menu ;;
  *Hyprland*) open_in_editor ~/.config/hypr/looknfeel.conf ;;
  *Screensaver*) show_screensaver_menu ;;
  *About*) show_about_menu ;;
  *) show_main_menu ;;
  esac
}

show_about_menu() {
  case $(menu "About" "  Edit Text\n  Set From Image\n  Restore Default") in
  *Text*) akatsuki-branding-about text ;;
  *Image*) akatsuki-branding-about image ;;
  *Default*) akatsuki-branding-about reset ;;
  *) show_style_menu ;;
  esac
}

show_screensaver_menu() {
  case $(menu "Screensaver" "  Edit Text\n  Set From Image\n  Restore Default") in
  *Text*) akatsuki-branding-screensaver text ;;
  *Image*) akatsuki-branding-screensaver image ;;
  *Default*) akatsuki-branding-screensaver reset ;;
  *) show_style_menu ;;
  esac
}

show_theme_menu() {
  akatsuki-launch-walker -m menus:akatsukithemes --width 800 --minheight 400
}

show_background_menu() {
  akatsuki-launch-walker -m menus:akatsukiBackgroundSelector --width 800 --minheight 400
}

show_font_menu() {
  theme=$(menu "Font" "$(akatsuki-font-list)" "--width 350" "$(akatsuki-font-current)")
  if [[ $theme == "CNCLD" || -z $theme ]]; then
    back_to show_style_menu
  else
    akatsuki-font-set "$theme"
  fi
}

show_setup_menu() {
  local options="  Audio\n  Wifi\n󰂯  Bluetooth\n󱐋  Power Profile\n  System Sleep\n󰍹  Monitors"
  [[ -f ~/.config/hypr/bindings.conf ]] && options="$options\n  Keybindings"
  [[ -f ~/.config/hypr/input.conf ]] && options="$options\n  Input"
  options="$options\n  Defaults\n󰱔  DNS\n  Security\n  Config"

  case $(menu "Setup" "$options") in
  *Audio*) akatsuki-launch-audio ;;
  *Wifi*) akatsuki-launch-wifi ;;
  *Bluetooth*) akatsuki-launch-bluetooth ;;
  *Power*) show_setup_power_menu ;;
  *System*) show_setup_system_menu ;;
  *Monitors*) open_in_editor ~/.config/hypr/monitors.conf ;;
  *Keybindings*) open_in_editor ~/.config/hypr/bindings.conf ;;
  *Input*) open_in_editor ~/.config/hypr/input.conf ;;
  *Defaults*) show_setup_default_menu ;;
  *DNS*) present_terminal akatsuki-setup-dns ;;
  *Security*) show_setup_security_menu ;;
  *Config*) show_setup_config_menu ;;
  *) show_main_menu ;;
  esac
}

show_setup_power_menu() {
  profile=$(menu "Power Profile" "$(akatsuki-powerprofiles-list)" "" "$(powerprofilesctl get)")

  if [[ $profile == "CNCLD" || -z $profile ]]; then
    back_to show_setup_menu
  else
    powerprofilesctl set "$profile"
  fi
}

show_setup_security_menu() {
  case $(menu "Setup" "󰈷  Fingerprint\n  Fido2") in
  *Fingerprint*) present_terminal akatsuki-setup-security-fingerprint ;;
  *Fido2*) present_terminal akatsuki-setup-security-fido2 ;;
  *) show_setup_menu ;;
  esac
}

show_setup_default_menu() {
  case $(menu "Default" "  Browser\n  Terminal\n  Editor") in
  *Browser*) show_setup_default_browser_menu ;;
  *Terminal*) show_setup_default_terminal_menu ;;
  *Editor*) show_setup_default_editor_menu ;;
  *) show_setup_menu ;;
  esac
}

browser_desktop_exists() {
  [[ -f ~/.local/share/applications/$1 || -f ~/.nix-profile/share/applications/$1 || -f /usr/share/applications/$1 ]]
}

show_setup_default_browser_menu() {
  local options=""
  browser_desktop_exists chromium.desktop && options="$options  Chromium"
  browser_desktop_exists google-chrome.desktop && options="${options:+$options\n}󰊯  Chrome"
  browser_desktop_exists brave-browser.desktop && options="${options:+$options\n}󰖟  Brave"
  browser_desktop_exists brave-origin-beta.desktop && options="${options:+$options\n}󰖟  Brave Origin"
  browser_desktop_exists microsoft-edge.desktop && options="${options:+$options\n}󰇩  Edge"
  browser_desktop_exists firefox.desktop && options="${options:+$options\n}󰈹  Firefox"
  browser_desktop_exists zen.desktop && options="${options:+$options\n}󰖟  Zen"

  local current=""
  case "$(akatsuki-default-browser)" in
  chromium) current="  Chromium" ;;
  chrome) current="󰊯  Chrome" ;;
  brave) current="󰖟  Brave" ;;
  brave-origin) current="󰖟  Brave Origin" ;;
  edge) current="󰇩  Edge" ;;
  firefox) current="󰈹  Firefox" ;;
  zen) current="󰖟  Zen" ;;
  esac

  case $(menu "Default Browser" "$options" "" "$current") in
  *Chromium*) akatsuki-default-browser chromium ;;
  *Chrome*) akatsuki-default-browser chrome ;;
  *"Brave Origin"*) akatsuki-default-browser brave-origin ;;
  *Brave*) akatsuki-default-browser brave ;;
  *Edge*) akatsuki-default-browser edge ;;
  *Firefox*) akatsuki-default-browser firefox ;;
  *Zen*) akatsuki-default-browser zen ;;
  *) show_setup_default_menu ;;
  esac
}

show_setup_default_terminal_menu() {
  local options=""
  akatsuki-cmd-present alacritty && options="$options  Alacritty"
  akatsuki-cmd-present foot && options="${options:+$options\n}  Foot"
  akatsuki-cmd-present ghostty && options="${options:+$options\n}  Ghostty"
  akatsuki-cmd-present kitty && options="${options:+$options\n}  Kitty"

  local current=""
  case "$(akatsuki-default-terminal)" in
  alacritty) current="  Alacritty" ;;
  foot) current="  Foot" ;;
  ghostty) current="  Ghostty" ;;
  kitty) current="  Kitty" ;;
  esac

  case $(menu "Default Terminal" "$options" "" "$current") in
  *Alacritty*) akatsuki-default-terminal alacritty ;;
  *Foot*) akatsuki-default-terminal foot ;;
  *Ghostty*) akatsuki-default-terminal ghostty ;;
  *Kitty*) akatsuki-default-terminal kitty ;;
  *) show_setup_default_menu ;;
  esac
}

show_setup_default_editor_menu() {
  local options=""
  akatsuki-cmd-present nvim && options="$options  Neovim"
  akatsuki-cmd-present code && options="${options:+$options\n}  VSCode"
  akatsuki-cmd-present cursor && options="${options:+$options\n}  Cursor"
  akatsuki-cmd-present zeditor && options="${options:+$options\n}  Zed"
  akatsuki-cmd-present sublime_text && options="${options:+$options\n}  Sublime Text"
  akatsuki-cmd-present helix && options="${options:+$options\n}  Helix"
  akatsuki-cmd-present vim && options="${options:+$options\n}  Vim"
  akatsuki-cmd-present emacs && options="${options:+$options\n}  Emacs"

  local current=""
  case "$(akatsuki-default-editor)" in
  nvim) current="  Neovim" ;;
  code) current="  VSCode" ;;
  cursor) current="  Cursor" ;;
  zed | zeditor) current="  Zed" ;;
  sublime_text) current="  Sublime Text" ;;
  helix) current="  Helix" ;;
  vim) current="  Vim" ;;
  emacs) current="  Emacs" ;;
  esac

  case $(menu "Default Editor" "$options" "" "$current") in
  *Neovim*) akatsuki-default-editor nvim ;;
  *VSCode*) akatsuki-default-editor code ;;
  *Cursor*) akatsuki-default-editor cursor ;;
  *Zed*) akatsuki-default-editor zed ;;
  *Sublime*) akatsuki-default-editor sublime_text ;;
  *Helix*) akatsuki-default-editor helix ;;
  *Vim*) akatsuki-default-editor vim ;;
  *Emacs*) akatsuki-default-editor emacs ;;
  *) show_setup_default_menu ;;
  esac
}

show_setup_config_menu() {
  case $(menu "Setup" "  Hyprland\n  Hypridle\n  Hyprlock\n  Hyprsunset\n  Swayosd\n󰌧  Walker\n󰍜  Waybar\n󰞅  XCompose") in
  *Hyprland*) open_in_editor ~/.config/hypr/hyprland.conf ;;
  *Hypridle*) open_in_editor ~/.config/hypr/hypridle.conf && akatsuki-restart-hypridle ;;
  *Hyprlock*) open_in_editor ~/.config/hypr/hyprlock.conf ;;
  *Hyprsunset*) open_in_editor ~/.config/hypr/hyprsunset.conf && akatsuki-restart-hyprsunset ;;
  *Swayosd*) open_in_editor ~/.config/swayosd/config.toml && akatsuki-restart-swayosd ;;
  *Walker*) open_in_editor ~/.config/walker/config.toml && akatsuki-restart-walker ;;
  *Waybar*) open_in_editor ~/.config/waybar/config.jsonc && akatsuki-restart-waybar ;;
  *XCompose*) open_in_editor ~/.XCompose && akatsuki-restart-xcompose ;;
  *) show_setup_menu ;;
  esac
}

show_setup_system_menu() {
  local options=""

  if akatsuki-toggle-enabled suspend-off; then
    options="$options󰒲  Enable Suspend"
  else
    options="$options󰒲  Disable Suspend"
  fi

  if akatsuki-hibernation-available; then
    options="$options\n󰤁  Disable Hibernate"
  else
    options="$options\n󰤁  Enable Hibernate"
  fi

  case $(menu "System" "$options") in
  *Suspend*) akatsuki-toggle-suspend ;;
  *"Enable Hibernate"*) present_terminal akatsuki-hibernation-setup ;;
  *"Disable Hibernate"*) present_terminal akatsuki-hibernation-remove ;;
  *) show_setup_menu ;;
  esac
}

show_install_menu() {
  case $(menu "Install" "󰣇  Package\n󰣇  AUR\n  Web App\n  TUI\n  Service\n  Style\n󰵮  Development\n  Editor\n  Terminal\n  Browser\n󱚤  AI\n  Gaming\n󰍲  Windows") in
  *Package*) terminal akatsuki-pkg-install ;;
  *AUR*) terminal akatsuki-pkg-aur-install ;;
  *Web*) present_terminal akatsuki-webapp-install ;;
  *TUI*) present_terminal akatsuki-tui-install ;;
  *Service*) show_install_service_menu ;;
  *Style*) show_install_style_menu ;;
  *Development*) show_install_development_menu ;;
  *Editor*) show_install_editor_menu ;;
  *Terminal*) show_install_terminal_menu ;;
  *Browser*) show_install_browser_menu ;;
  *Gaming*) show_install_gaming_menu ;;
  *AI*) show_install_ai_menu ;;
  *Windows*) present_terminal "akatsuki-windows-vm install" ;;
  *) show_main_menu ;;
  esac
}

show_install_browser_menu() {
  case $(menu "Install" "  Chrome\n  Edge\n  Brave\n  Brave Origin\n  Firefox\n󰖟  Zen") in
  *Chrome*) present_terminal "akatsuki-install-browser chrome" ;;
  *Edge*) present_terminal "akatsuki-install-browser edge" ;;
  *"Brave Origin"*) present_terminal "akatsuki-install-browser brave-origin" ;;
  *Brave*) present_terminal "akatsuki-install-browser brave" ;;
  *Firefox*) present_terminal "akatsuki-install-browser firefox" ;;
  *Zen*) present_terminal "akatsuki-install-browser zen" ;;
  *) show_install_menu ;;
  esac
}

show_install_service_menu() {
  case $(menu "Install" "  Dropbox\n  Tailscale\n󱇱  NordVPN [AUR]\n󰏖  ONCE\n󰟵  Bitwarden\n  Chromium Account") in
  *Dropbox*) present_terminal akatsuki-install-dropbox ;;
  *Tailscale*) present_terminal akatsuki-install-tailscale ;;
  *NordVPN*) present_terminal akatsuki-install-nordvpn ;;
  *ONCE*) present_terminal akatsuki-install-once ;;
  *Bitwarden*) install_and_launch "Bitwarden" "bitwarden bitwarden-cli" "bitwarden" ;;
  *Chromium*) present_terminal akatsuki-install-chromium-google-account ;;
  *) show_install_menu ;;
  esac
}

show_install_editor_menu() {
  case $(menu "Install" "  VSCode\n  Cursor\n  Zed\n  Sublime Text\n  Helix\n  Vim\n  Emacs") in
  *VSCode*) present_terminal akatsuki-install-vscode ;;
  *Cursor*) install_and_launch "Cursor" "cursor-bin" "cursor" ;;
  *Zed*) present_terminal akatsuki-install-zed ;;
  *Sublime*) install_and_launch "Sublime Text" "sublime-text-4" "sublime_text" ;;
  *Helix*) present_terminal akatsuki-install-helix ;;
  *Vim*) install "Vim" "vim" ;;
  *Emacs*) install "Emacs" "emacs-wayland" && systemctl --user enable --now emacs.service ;;
  *) show_install_menu ;;
  esac
}

show_install_terminal_menu() {
  case $(menu "Install" "  Alacritty\n  Foot\n  Ghostty\n  Kitty") in
  *Alacritty*) install_terminal "alacritty" ;;
  *Foot*) install_terminal "foot" ;;
  *Ghostty*) install_terminal "ghostty" ;;
  *Kitty*) install_terminal "kitty" ;;
  *) show_install_menu ;;
  esac
}

show_install_ai_menu() {
  ollama_pkg=$(
    (akatsuki-cmd-present nvidia-smi && echo ollama-cuda) ||
      (akatsuki-cmd-present rocminfo && echo ollama-rocm) ||
      echo ollama
  )

  case $(menu "Install" "  Dictation\n󱚤  LM Studio\n󱚤  Ollama\n󱚤  Crush") in
  *Dictation*) present_terminal akatsuki-voxtype-install ;;
  *Studio*) install "LM Studio" "lmstudio-bin" ;;
  *Ollama*) install "Ollama" $ollama_pkg ;;
  *Crush*) install "Crush" "crush-bin" ;;
  *) show_install_menu ;;
  esac
}

show_install_gaming_menu() {
  case $(menu "Install" "  Steam\n  RetroArch\n󰍳  Minecraft\n󰢹  NVIDIA GeForce NOW\n  Xbox Cloud Gaming\n󰂯  Xbox Controller\n󰍹  Moonlight (GameStream)\n  Lutris (Battle.net)\n󱓟  Heroic (Epic Games)") in
  *Steam*) present_terminal akatsuki-install-gaming-steam ;;
  *RetroArch*) present_terminal akatsuki-install-gaming-retroarch ;;
  *Minecraft*) install_and_launch "Minecraft" "minecraft-launcher" "minecraft-launcher" ;;
  *GeForce*) present_terminal akatsuki-install-gaming-geforce-now ;;
  *"Xbox Cloud"*) present_terminal akatsuki-install-gaming-xbox-cloud ;;
  *Xbox*) present_terminal akatsuki-install-gaming-xbox-controllers ;;
  *Lutris*) present_terminal akatsuki-install-gaming-lutris ;;
  *Heroic*) present_terminal akatsuki-install-gaming-heroic ;;
  *Moonlight*) present_terminal akatsuki-install-gaming-moonlight ;;
  *) show_install_menu ;;
  esac
}

show_install_style_menu() {
  case $(menu "Install" "󰸌  Theme\n  Background\n  Font") in
  *Theme*) present_terminal akatsuki-theme-install ;;
  *Background*) akatsuki-theme-bg-install ;;
  *Font*) show_install_font_menu ;;
  *) show_install_menu ;;
  esac
}

show_install_font_menu() {
  case $(menu "Install" "  Cascadia Mono\n  Meslo LG Mono\n  Fira Code\n  Victor Code\n  Bitstream Vera Mono\n  Iosevka" "--width 350") in
  *Cascadia*) install_font "Cascadia Mono" "ttf-cascadia-mono-nerd" "CaskaydiaMono Nerd Font" ;;
  *Meslo*) install_font "Meslo LG Mono" "ttf-meslo-nerd" "MesloLGL Nerd Font" ;;
  *Fira*) install_font "Fira Code" "ttf-firacode-nerd" "FiraCode Nerd Font" ;;
  *Victor*) install_font "Victor Code" "ttf-victor-mono-nerd" "VictorMono Nerd Font" ;;
  *Bitstream*) install_font "Bitstream Vera Code" "ttf-bitstream-vera-mono-nerd" "BitstromWera Nerd Font" ;;
  *Iosevka*) install_font "Iosevka" "ttf-iosevka-nerd" "Iosevka Nerd Font Mono" ;;
  *) show_install_menu ;;
  esac
}

show_install_development_menu() {
  case $(menu "Install" "󰫏  Ruby on Rails\n  Docker DB\n  JavaScript\n  Go\n  PHP\n  Python\n  Elixir\n  Zig\n  Rust\n  Java\n  .NET\n  OCaml\n  Clojure\n  Scala") in
  *Rails*) present_terminal "akatsuki-install-dev-env ruby" ;;
  *Docker*) present_terminal akatsuki-install-docker-dbs ;;
  *JavaScript*) show_install_javascript_menu ;;
  *Go*) present_terminal "akatsuki-install-dev-env go" ;;
  *PHP*) show_install_php_menu ;;
  *Python*) present_terminal "akatsuki-install-dev-env python" ;;
  *Elixir*) show_install_elixir_menu ;;
  *Zig*) present_terminal "akatsuki-install-dev-env zig" ;;
  *Rust*) present_terminal "akatsuki-install-dev-env rust" ;;
  *Java*) present_terminal "akatsuki-install-dev-env java" ;;
  *NET*) present_terminal "akatsuki-install-dev-env dotnet" ;;
  *OCaml*) present_terminal "akatsuki-install-dev-env ocaml" ;;
  *Clojure*) present_terminal "akatsuki-install-dev-env clojure" ;;
  *Scala*) present_terminal "akatsuki-install-dev-env scala" ;;
  *) show_install_menu ;;
  esac
}

show_install_javascript_menu() {
  case $(menu "Install" "  Node.js\n  Bun\n  Deno") in
  *Node*) present_terminal "akatsuki-install-dev-env node" ;;
  *Bun*) present_terminal "akatsuki-install-dev-env bun" ;;
  *Deno*) present_terminal "akatsuki-install-dev-env deno" ;;
  *) show_install_development_menu ;;
  esac
}

show_install_php_menu() {
  case $(menu "Install" "  PHP\n  Laravel\n  Symfony") in
  *PHP*) present_terminal "akatsuki-install-dev-env php" ;;
  *Laravel*) present_terminal "akatsuki-install-dev-env laravel" ;;
  *Symfony*) present_terminal "akatsuki-install-dev-env symfony" ;;
  *) show_install_development_menu ;;
  esac
}

show_install_elixir_menu() {
  case $(menu "Install" "  Elixir\n  Phoenix") in
  *Elixir*) present_terminal "akatsuki-install-dev-env elixir" ;;
  *Phoenix*) present_terminal "akatsuki-install-dev-env phoenix" ;;
  *) show_install_development_menu ;;
  esac
}

show_remove_menu() {
  case $(menu "Remove" "󰣇  Package\n  Web App\n  TUI\n󰵮  Development\n󰸌  Theme\n  Browser\n  Dictation\n  Gaming\n󰍲  Windows\n󰏓  Preinstalls\n  Security") in
  *Package*) terminal akatsuki-pkg-remove ;;
  *Web*) present_terminal akatsuki-webapp-remove ;;
  *TUI*) present_terminal akatsuki-tui-remove ;;
  *Development*) show_remove_development_menu ;;
  *Theme*) present_terminal akatsuki-theme-remove ;;
  *Browser*) show_remove_browser_menu ;;
  *Dictation*) present_terminal akatsuki-voxtype-remove ;;
  *Gaming*) show_remove_gaming_menu ;;
  *Windows*) present_terminal "akatsuki-windows-vm remove" ;;
  *Preinstalls*) present_terminal akatsuki-remove-preinstalls ;;
  *Security*) show_remove_security_menu ;;
  *) show_main_menu ;;
  esac
}

show_remove_security_menu() {
  case $(menu "Remove" "󰈷  Fingerprint\n  Fido2") in
  *Fingerprint*) present_terminal akatsuki-remove-security-fingerprint ;;
  *Fido2*) present_terminal akatsuki-remove-security-fido2 ;;
  *) show_remove_menu ;;
  esac
}

show_remove_browser_menu() {
  case $(menu "Remove" "  Chrome\n  Edge\n  Brave\n  Brave Origin\n  Firefox\n  Zen") in
  *Chrome*) present_terminal "akatsuki-remove-browser chrome" ;;
  *Edge*) present_terminal "akatsuki-remove-browser edge" ;;
  *"Brave Origin"*) present_terminal "akatsuki-remove-browser brave-origin" ;;
  *Brave*) present_terminal "akatsuki-remove-browser brave" ;;
  *Firefox*) present_terminal "akatsuki-remove-browser firefox" ;;
  *Zen*) present_terminal "akatsuki-remove-browser zen" ;;
  *) show_remove_menu ;;
  esac
}

show_remove_gaming_menu() {
  case $(menu "Remove" "  Steam\n  RetroArch\n󰍳  Minecraft\n󰢹  NVIDIA GeForce NOW\n  Xbox Cloud Gaming\n󰖺  Xbox Controller (󰂯)\n󰍹  Moonlight (GameStream)\n  Lutris (Battle.net)\n󱓟  Heroic (Epic Games)") in
  *Steam*) present_terminal akatsuki-remove-gaming-steam ;;
  *RetroArch*) present_terminal akatsuki-remove-gaming-retroarch ;;
  *Minecraft*) present_terminal akatsuki-remove-gaming-minecraft ;;
  *GeForce*) present_terminal akatsuki-remove-gaming-geforce-now ;;
  *"Xbox Cloud"*) present_terminal akatsuki-remove-gaming-xbox-cloud ;;
  *Xbox*) present_terminal akatsuki-remove-gaming-xbox-controllers ;;
  *Moonlight*) present_terminal akatsuki-remove-gaming-moonlight ;;
  *Lutris*) present_terminal akatsuki-remove-gaming-lutris ;;
  *Heroic*) present_terminal akatsuki-remove-gaming-heroic ;;
  *) show_remove_menu ;;
  esac
}

show_remove_development_menu() {
  case $(menu "Remove" "󰫏  Ruby on Rails\n  JavaScript\n  Go\n  PHP\n  Python\n  Elixir\n  Zig\n  Rust\n  Java\n  .NET\n  OCaml\n  Clojure\n  Scala") in
  *Rails*) present_terminal "akatsuki-remove-dev-env ruby" ;;
  *JavaScript*) show_remove_javascript_menu ;;
  *Go*) present_terminal "akatsuki-remove-dev-env go" ;;
  *PHP*) show_remove_php_menu ;;
  *Python*) present_terminal "akatsuki-remove-dev-env python" ;;
  *Elixir*) show_remove_elixir_menu ;;
  *Zig*) present_terminal "akatsuki-remove-dev-env zig" ;;
  *Rust*) present_terminal "akatsuki-remove-dev-env rust" ;;
  *Java*) present_terminal "akatsuki-remove-dev-env java" ;;
  *NET*) present_terminal "akatsuki-remove-dev-env dotnet" ;;
  *OCaml*) present_terminal "akatsuki-remove-dev-env ocaml" ;;
  *Clojure*) present_terminal "akatsuki-remove-dev-env clojure" ;;
  *Scala*) present_terminal "akatsuki-remove-dev-env scala" ;;
  *) show_remove_menu ;;
  esac
}

show_remove_javascript_menu() {
  case $(menu "Remove" "  Node.js\n  Bun\n  Deno") in
  *Node*) present_terminal "akatsuki-remove-dev-env node" ;;
  *Bun*) present_terminal "akatsuki-remove-dev-env bun" ;;
  *Deno*) present_terminal "akatsuki-remove-dev-env deno" ;;
  *) show_remove_development_menu ;;
  esac
}

show_remove_php_menu() {
  case $(menu "Remove" "  PHP\n  Laravel\n  Symfony") in
  *PHP*) present_terminal "akatsuki-remove-dev-env php" ;;
  *Laravel*) present_terminal "akatsuki-remove-dev-env laravel" ;;
  *Symfony*) present_terminal "akatsuki-remove-dev-env symfony" ;;
  *) show_remove_development_menu ;;
  esac
}

show_remove_elixir_menu() {
  case $(menu "Remove" "  Elixir\n  Phoenix") in
  *Elixir*) present_terminal "akatsuki-remove-dev-env elixir" ;;
  *Phoenix*) present_terminal "akatsuki-remove-dev-env phoenix" ;;
  *) show_remove_development_menu ;;
  esac
}

show_update_menu() {
  case $(menu "Update" "  Akatsuki\n󰔫  Channel\n  Config\n󰸌  Extra Themes\n  Process\n󰇅  Hardware\n  Firmware\n  Password\n  Timezone\n  Time") in
  *Akatsuki*) present_terminal akatsuki-update ;;
  *Channel*) show_update_channel_menu ;;
  *Config*) show_update_config_menu ;;
  *Themes*) present_terminal akatsuki-theme-update ;;
  *Process*) show_update_process_menu ;;
  *Hardware*) show_update_hardware_menu ;;
  *Firmware*) present_terminal akatsuki-update-firmware ;;
  *Timezone*) present_terminal akatsuki-tz-select ;;
  *Time*) present_terminal akatsuki-update-time ;;
  *Password*) show_update_password_menu ;;
  *) show_main_menu ;;
  esac
}

show_update_channel_menu() {
  case $(menu "Update channel" "🟢 Stable\n🟡 RC\n🟠 Edge\n🔴 Dev") in
  *Stable*) present_terminal "akatsuki-channel-set stable" ;;
  *RC*) present_terminal "akatsuki-channel-set rc" ;;
  *Edge*) present_terminal "akatsuki-channel-set edge" ;;
  *Dev*) present_terminal "akatsuki-channel-set dev" ;;
  *) show_update_menu ;;
  esac
}
show_update_process_menu() {
  case $(menu "Restart" "  Hypridle\n  Hyprsunset\n󰎟  Mako\n  Swayosd\n󰌧  Walker\n󰍜  Waybar") in
  *Hypridle*) akatsuki-restart-hypridle ;;
  *Hyprsunset*) akatsuki-restart-hyprsunset ;;
  *Mako*) akatsuki-restart-mako ;;
  *Swayosd*) akatsuki-restart-swayosd ;;
  *Walker*) akatsuki-restart-walker ;;
  *Waybar*) akatsuki-restart-waybar ;;
  *) show_update_menu ;;
  esac
}

show_update_config_menu() {
  case $(menu "Use default config" "  Hyprland\n  Hypridle\n  Hyprlock\n  Hyprsunset\n󱣴  Plymouth\n  Swayosd\n  Tmux\n󰌧  Walker\n󰍜  Waybar") in
  *Hyprland*) present_terminal akatsuki-refresh-hyprland ;;
  *Hypridle*) present_terminal akatsuki-refresh-hypridle ;;
  *Hyprlock*) present_terminal akatsuki-refresh-hyprlock ;;
  *Hyprsunset*) present_terminal akatsuki-refresh-hyprsunset ;;
  *Plymouth*) present_terminal akatsuki-refresh-plymouth ;;
  *Swayosd*) present_terminal akatsuki-refresh-swayosd ;;
  *Tmux*) present_terminal akatsuki-refresh-tmux ;;
  *Walker*) present_terminal akatsuki-refresh-walker ;;
  *Waybar*) present_terminal akatsuki-refresh-waybar ;;
  *) show_update_menu ;;
  esac
}

show_update_hardware_menu() {
  case $(menu "Restart" "  Audio\n󱚾  Wi-Fi\n󰂯  Bluetooth\n󰟸  Trackpad") in
  *Audio*) present_terminal akatsuki-restart-pipewire ;;
  *Wi-Fi*) present_terminal akatsuki-restart-wifi ;;
  *Bluetooth*) present_terminal akatsuki-restart-bluetooth ;;
  *Trackpad*) present_terminal akatsuki-restart-trackpad ;;
  *) show_update_menu ;;
  esac
}

show_update_password_menu() {
  case $(menu "Update Password" "  Drive Encryption\n  User") in
  *Drive*) present_terminal akatsuki-drive-password ;;
  *User*) present_terminal passwd ;;
  *) show_update_menu ;;
  esac
}

show_about() {
  akatsuki-launch-about
}

show_system_menu() {
  local options="󱄄  Screensaver\n  Lock"
  ! akatsuki-toggle-enabled suspend-off && options="$options\n󰒲  Suspend"
  akatsuki-hibernation-available && options="$options\n󰤁  Hibernate"
  options="$options\n󰍃  Logout\n󰜉  Restart\n󰐥  Shutdown"

  case $(menu "System" "$options") in
  *Screensaver*) akatsuki-launch-screensaver force ;;
  *Lock*) akatsuki-system-lock ;;
  *Suspend*) systemctl suspend ;;
  *Hibernate*) systemctl hibernate ;;
  *Logout*) akatsuki-system-logout ;;
  *Restart*) akatsuki-system-reboot ;;
  *Shutdown*) akatsuki-system-shutdown ;;
  *) back_to show_main_menu ;;
  esac
}

show_main_menu() {
  go_to_menu "$(menu "Go" "󰀻  Apps\n󰧑  Learn\n󱓞  Trigger\n  Style\n  Setup\n󰉉  Install\n󰭌  Remove\n  Update\n  About\n  System")"
}

go_to_menu() {
  case "${1,,}" in
  *apps*) walker -p "Launch…" ;;
  *learn*) show_learn_menu ;;
  *trigger*) show_trigger_menu ;;
  *toggle*) show_toggle_menu ;;
  *hardware*) show_hardware_menu ;;
  *share*) show_share_menu ;;
  *reminder-set*) show_custom_reminder_input ;;
  *reminder*) show_reminder_menu ;;
  *background*) show_background_menu ;;
  *capture*) show_capture_menu ;;
  *style*) show_style_menu ;;
  *theme*) show_theme_menu ;;
  *screenrecord*) show_screenrecord_menu ;;
  *setup*) show_setup_menu ;;
  *power*) show_setup_power_menu ;;
  *install*) show_install_menu ;;
  *remove*) show_remove_menu ;;
  *update*) show_update_menu ;;
  *about*) show_about ;;
  *system*) show_system_menu ;;
  esac
}

# Allow user extensions and overrides
USER_EXTENSIONS="$HOME/.config/akatsuki/extensions/menu.sh"
[[ -f $USER_EXTENSIONS ]] && source "$USER_EXTENSIONS"

toggle_existing_menu

if [[ -n $1 ]]; then
  BACK_TO_EXIT=true
  go_to_menu "$1"
else
  show_main_menu
fi
