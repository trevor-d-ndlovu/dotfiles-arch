#!/bin/bash

# akatsuki:summary=Finish the installation of Akatsuki with items that can only be done after logging in.
# akatsuki:requires-sudo=true

set -e

FIRST_RUN_MODE=~/.local/state/akatsuki/first-run.mode

if [[ -f $FIRST_RUN_MODE ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$AKATSUKI_PATH/install/first-run/battery-monitor.sh"
  bash "$AKATSUKI_PATH/install/first-run/recover-internal-monitor.sh"
  bash "$AKATSUKI_PATH/install/first-run/cleanup-reboot-sudoers.sh"
  bash "$AKATSUKI_PATH/install/first-run/firewall.sh"
  bash "$AKATSUKI_PATH/install/first-run/dns-resolver.sh"
  bash "$AKATSUKI_PATH/install/first-run/gnome-theme.sh"
  bash "$AKATSUKI_PATH/install/first-run/swayosd.sh"
  bash "$AKATSUKI_PATH/install/first-run/gtk-primary-paste.sh"
  bash "$AKATSUKI_PATH/install/first-run/elephant.sh"
  akatsuki-hook-install post-update "$AKATSUKI_PATH/install/first-run/install-voxtype.hook"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$AKATSUKI_PATH/install/first-run/welcome.sh"
  bash "$AKATSUKI_PATH/install/first-run/wifi.sh"
fi
