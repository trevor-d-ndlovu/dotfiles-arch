#!/bin/bash

# akatsuki:summary=Sync Akatsuki theme to all Obsidian vaults
# akatsuki:hidden=true

CURRENT_THEME_DIR="$HOME/.config/akatsuki/current/theme"

[[ -f $CURRENT_THEME_DIR/obsidian.css ]] || exit 0

jq -r '.vaults | values[].path' ~/.config/obsidian/obsidian.json 2>/dev/null | while read -r vault_path; do
  [[ -d $vault_path/.obsidian ]] || continue

  theme_dir="$vault_path/.obsidian/themes/Akatsuki"
  mkdir -p "$theme_dir"

  [[ -f $theme_dir/manifest.json ]] || cat >"$theme_dir/manifest.json" <<'EOF'
{
  "name": "Akatsuki",
  "version": "1.0.0",
  "minAppVersion": "0.16.0",
  "description": "Automatically syncs with your current Akatsuki system theme colors and fonts",
  "author": "Akatsuki",
  "authorUrl": "https://akatsuki.org"
}
EOF

  cp "$CURRENT_THEME_DIR/obsidian.css" "$theme_dir/theme.css"
done
