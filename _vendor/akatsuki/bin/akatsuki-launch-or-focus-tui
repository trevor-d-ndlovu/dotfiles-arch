#!/bin/bash

# akatsuki:summary=Launch a TUI or focus an existing terminal window for it
# akatsuki:args=<command> [args...]

APP_ID="org.akatsuki.$(basename "$1")"
LAUNCH_COMMAND="akatsuki-launch-tui $@"

exec akatsuki-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
