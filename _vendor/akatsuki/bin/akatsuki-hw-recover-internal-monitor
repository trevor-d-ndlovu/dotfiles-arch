#!/bin/bash

# akatsuki:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/akatsuki/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! akatsuki-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
