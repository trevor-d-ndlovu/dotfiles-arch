#!/bin/bash

# akatsuki:summary=Set the branch for Akatsuki's git repository.
# akatsuki:args=<master|rc|dev>

if (($# == 0)); then
  echo "Usage: akatsuki-branch-set [master|rc|dev]"
  exit 1
else
  branch="$1"
fi

if [[ $branch != "master" && $branch != "rc" && $branch != "dev" ]]; then
  echo "Error: Invalid branch '$branch'. Must be one of: master, rc, dev"
  exit 1
fi

git -C $AKATSUKI_PATH switch $branch
