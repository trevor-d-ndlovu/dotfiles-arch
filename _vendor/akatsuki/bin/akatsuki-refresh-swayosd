#!/bin/bash

# akatsuki:summary=Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the Akatsuki defaults and restart the service.

akatsuki-refresh-config swayosd/config.toml
akatsuki-refresh-config swayosd/style.css
akatsuki-restart-swayosd
