#!/bin/bash

# akatsuki:summary=Toggle Waybar visibility
# akatsuki:examples=akatsuki toggle waybar

akatsuki-toggle waybar-off

if pgrep -x waybar >/dev/null; then
  pkill -x waybar
else
  uwsm app -- waybar >/dev/null 2>&1 &
fi
