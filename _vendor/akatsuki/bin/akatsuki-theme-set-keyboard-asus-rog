#!/bin/bash

# akatsuki:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# akatsuki:hidden=true

ASUSCTL_THEME=~/.config/akatsuki/current/theme/keyboard.rgb

if akatsuki-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
