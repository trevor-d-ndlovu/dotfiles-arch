#!/bin/bash

# akatsuki:summary=Shut down after closing application windows
# akatsuki:examples=akatsuki shutdown | akatsuki system shutdown
# akatsuki:aliases=akatsuki shutdown

akatsuki-state clear re*-required

# Schedule the shutdown to happen after closing windows (detached from terminal)
nohup bash -c "sleep 2 && systemctl poweroff --no-wall" >/dev/null 2>&1 &

# Now close all windows
akatsuki-hyprland-window-close-all
sleep 1 # Allow apps like Chrome to shutdown correctly
