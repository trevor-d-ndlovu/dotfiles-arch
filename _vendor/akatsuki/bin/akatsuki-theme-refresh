#!/bin/bash

# akatsuki:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/akatsuki/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  AKATSUKI_THEME_SKIP_BACKGROUND=1 akatsuki-theme-set "$(cat $THEME_NAME_PATH)"
fi
